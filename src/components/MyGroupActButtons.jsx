import React from 'react';
import './MyGroupActButtons.css';

export default function MyGroupButtonsActions({ children }) {
  return (
    <div className="my-group-act-buttons-actions">
      {children}
    </div>
  );
}
