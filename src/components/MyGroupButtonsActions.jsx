import React from 'react';
import './MyGroupButtonsActions.css';

export default function MyGroupButtonsActions({ children }) {
  return (
    <div className="my-group-buttons-actions">
      {children}
    </div>
  );
}
